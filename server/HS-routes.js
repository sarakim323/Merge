const express = require('express');
const router = express.Router();

// get user's health screenings history (all)
router.get('/');

// post user's health screening history (1)
router.post('/');

// update user's health screenings history (1)
router.put('/')

// delete user's health screening history (1)

module.exports = router;